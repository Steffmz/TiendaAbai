// frontend/src/config.js

// Este archivo centraliza valores que se usan en múltiples lugares.
// Si necesitas cambiar cuántos productos se muestran por página,
// solo tienes que cambiarlo aquí.

export const PAGINATION = {
  CATEGORIES: 6,
  PRODUCTS: 6,
  CAMPAIGNS: 6,
  USERS: 6, // Un valor razonable para la tabla de usuarios
  PEDIDOS: 6,
};